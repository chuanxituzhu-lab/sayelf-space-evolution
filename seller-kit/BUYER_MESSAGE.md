# 发货后给买家的消息

收到文件后先解压 ZIP，不要直接在压缩包里运行。

Windows 用户：
1. 确认已安装 Python 3.10+；
2. 双击 `start_windows.bat`；
3. 浏览器自动打开后，上传手绘稿；
4. 点击「生成完整工作流」。

第一次使用请先看 `docs/QUICK_START.md`。

如果只想在自己常用的 AI 平台里使用，也可以直接读取 `skill/SKILL.md`，再把 WebUI 生成的阶段提示词交给对应图像/视频工具。
